# نظام حضور الموظفين وتقارير العمل

نظام متكامل لإدارة حضور الموظفين وتقارير العمل اليومية، مبني باستخدام Next.js و Supabase.

## 🚀 المميزات

### للموظفين
- ✅ تسجيل التقرير اليومي (عنوان الكتاب، صفحات الطباعة/التنضيد/التحرير)
- ✅ تسجيل الإجازات
- ✅ عرض سجل التقارير السابقة
- ✅ تعديل التقارير

### للمديرين
- ✅ إدارة الموظفين (إضافة، تعديل، تعطيل، حذف)
- ✅ عرض جميع التقارير مع فلاتر متقدمة
- ✅ تعديل تقارير الموظفين مع تسجيل التدقيق
- ✅ تحليلات ورسوم بيانية
- ✅ تصدير التقارير (Excel و PDF)

### عام
- 🌙 دعم الوضع الليلي والنهاري
- 🔐 نظام صلاحيات آمن (RLS)
- 📱 تصميم متجاوب (Mobile-first)
- 🇮🇶 واجهة عربية بالكامل (RTL)

## 🛠️ التقنيات المستخدمة

- **Frontend**: Next.js 14+ (App Router), TypeScript, TailwindCSS
- **Backend**: Supabase (Auth, Postgres, RLS)
- **Charts**: Recharts
- **Exports**: SheetJS (xlsx), pdf-lib

## 📋 متطلبات التشغيل

- Node.js 18+
- npm أو yarn
- حساب Supabase

## 🔧 خطوات الإعداد

### 1. إنشاء مشروع Supabase

1. اذهب إلى [supabase.com](https://supabase.com) وأنشئ مشروعاً جديداً
2. انتظر حتى يتم إنشاء المشروع

### 2. إعداد قاعدة البيانات

1. اذهب إلى **SQL Editor** في لوحة تحكم Supabase
2. انسخ محتوى ملف `sql/001_schema.sql` ونفّذه
3. انسخ محتوى ملف `sql/002_rls_policies.sql` ونفّذه

### 3. إعداد متغيرات البيئة

1. انسخ ملف `.env.example` إلى `.env.local`:
   ```bash
   cp .env.example .env.local
   ```

2. املأ المتغيرات من لوحة تحكم Supabase:
   - `NEXT_PUBLIC_SUPABASE_URL`: من Settings > API > Project URL
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`: من Settings > API > anon public
   - `SUPABASE_SERVICE_ROLE_KEY`: من Settings > API > service_role (⚠️ سري)

### 4. إنشاء حساب المدير الأول

بما أن إنشاء المستخدمين يتطلب صلاحية مدير، نحتاج لإنشاء أول مدير يدوياً:

1. اذهب إلى **Authentication > Users** في Supabase
2. اضغط **Add User** > **Create New User**
3. أدخل البريد الإلكتروني وكلمة المرور
4. اذهب إلى **SQL Editor** ونفّذ:
   ```sql
   INSERT INTO profiles (id, full_name, phone, role, is_active)
   VALUES (
     'USER_ID_FROM_AUTH', -- استبدل بـ ID المستخدم من جدول auth.users
     'اسم المدير',
     '07xxxxxxxxx',
     'admin',
     true
   );
   ```

### 5. تشغيل المشروع

```bash
# تثبيت الحزم
npm install

# تشغيل في وضع التطوير
npm run dev
```

افتح [http://localhost:3000](http://localhost:3000) في المتصفح.

## 📁 هيكل المشروع

```
employ/
├── app/
│   ├── (auth)/
│   │   └── login/          # صفحة تسجيل الدخول
│   ├── (dashboard)/
│   │   ├── employee/       # لوحة تحكم الموظف
│   │   └── admin/          # لوحة تحكم المدير
│   ├── actions/            # Server Actions
│   ├── globals.css         # الأنماط العامة
│   └── layout.tsx          # التخطيط الرئيسي
├── components/
│   ├── ui/                 # مكونات واجهة المستخدم
│   ├── layout/             # مكونات التخطيط
│   ├── employee/           # مكونات الموظف
│   └── providers/          # مزودات السياق
├── lib/
│   ├── supabase/           # عملاء Supabase
│   ├── types.ts            # أنواع TypeScript
│   ├── utils.ts            # دوال مساعدة
│   └── constants.ts        # ثوابت التطبيق
├── sql/                    # سكريبتات قاعدة البيانات
└── middleware.ts           # وسيط المصادقة
```

## 🔐 الأمان

### سياسات RLS

- **جدول profiles**:
  - المستخدم يرى ويعدّل ملفه الشخصي فقط
  - المدير يرى ويعدّل جميع الملفات

- **جدول daily_reports**:
  - الموظف يدير تقاريره فقط
  - المدير يدير جميع التقارير

- **جدول audit_logs**:
  - المدير فقط يقرأ سجلات التدقيق

## 📊 قاعدة البيانات

### الجداول

1. **profiles** - بيانات الموظفين
2. **daily_reports** - التقارير اليومية
3. **audit_logs** - سجلات التدقيق

### القيود

- تقرير واحد لكل موظف في اليوم (UNIQUE)
- قيم الصفحات لا يمكن أن تكون سالبة
- الدور محدود بـ 'admin' أو 'employee'

## ✅ قائمة الاختبار

### المصادقة
- [ ] تسجيل دخول ناجح بالبيانات الصحيحة
- [ ] رسالة خطأ عند البيانات الخاطئة
- [ ] إعادة توجيه حسب الدور

### RLS
- [ ] الموظف لا يرى تقارير الآخرين
- [ ] المدير يرى جميع التقارير
- [ ] الموظف لا يستطيع إنشاء موظفين

### التقارير
- [ ] إنشاء تقرير جديد
- [ ] تحديث تقرير موجود
- [ ] تفعيل الإجازة يعطّل حقول الصفحات

### الإدارة
- [ ] إضافة موظف جديد
- [ ] تعديل بيانات موظف
- [ ] تعطيل/تفعيل موظف
- [ ] تعديل تقرير مع تسجيل التدقيق

### التصدير
- [ ] تصدير Excel صالح
- [ ] تصدير PDF صالح

### الواجهة
- [ ] تبديل الوضع الليلي/النهاري
- [ ] حفظ تفضيل الوضع
- [ ] عرض RTL صحيح

## 🚀 النشر

### Vercel (مُوصى به)

1. ارفع المشروع إلى GitHub
2. اذهب إلى [vercel.com](https://vercel.com)
3. اربط المستودع
4. أضف متغيرات البيئة
5. انشر!

### متغيرات البيئة للإنتاج

تأكد من إضافة جميع المتغيرات في إعدادات المشروع:
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY`

## 📝 ملاحظات

- المنطقة الزمنية: Asia/Baghdad (UTC+3)
- اللغة الافتراضية: العربية
- الخط: Noto Sans Arabic

## 🤝 المساهمة

نرحب بمساهماتكم! يرجى فتح Issue أو Pull Request.

## 📄 الرخصة

MIT License
